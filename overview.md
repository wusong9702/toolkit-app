# 跨平台应用方案 · 交付概览

**日期**：2026-09-01
**需求**：开发支持 iOS + Android 的工具类应用，用户为零基础新手，需后续连接 GitHub 并完成打包发布

---

## 一、方案结论

采用 **Vue 3 网页应用 + Capacitor 套壳** 的跨平台方案。

选择理由：工具类应用以列表、表单、按钮为主，网页技术与原生技术在用户体验上无差别，但学习成本相差数倍。用户为零基础，网页技术栈（HTML/CSS/JS 体系）能立即上手，且中文社区资料最丰富。

**分四阶段推进**，每阶段都有可验证的产出：

1. 网页版跑通（已完成脚手架）
2. 连 GitHub + 自动部署
3. 打包 Android
4. 打包 iOS + 上架

---

## 二、已完成的工作

### 1. 技术方案文档

`docs/01-技术方案.md` —— 选型对比、决策依据、四阶段路线图、成本清单、七个必知风险点。

核心结论：
- 最低成本 0 元（仅网页版阶段）
- 完整上架双端首年约 900 元，之后每年约 730 元
- iOS 构建必须 macOS，已用 GitHub Actions 云 Mac 绕开（用户只有 Windows）

### 2. 可运行的项目脚手架

`app/` 目录，技术栈 Vue 3 + Vite 6 + Vant 4 + Pinia + TypeScript + PWA + Capacitor 7。

**关键技术决策：**

| 决策 | 原因 |
|---|---|
| 路由用 hash 模式 | App 内是 file:// 协议加载，history 模式会白屏。这是新手最易踩的坑，已在源头规避 |
| 一开始就做 PWA | Apple 会以"就是个网页"为由拒审，离线可用性是过审必要条件 |
| 云端内容三级兜底 | 网络优先 → 本地缓存 → 内置数据，保证断网可用 |
| 数据本地持久化 | 工具类应用断网必须可用 |

**已实现的完整功能模块（记账）**，作为用户扩展自己功能的模板：

- 增删改查 + 滑动删除
- localStorage 持久化（数据变更自动保存）
- 分类占比统计（纯 CSS 实现，不引图表库，控制包体积）
- 按日期分组
- 原生震动反馈（浏览器环境自动降级）
- 云端分类数据拉取（改 JSON 即生效，无需发版）

**三个页面：**

- `HomeView.vue` —— 工具入口、云端公告、今日统计
- `LedgerView.vue` —— 完整记账模块
- `SettingsView.vue` —— 数据源配置、数据管理、运行环境识别

### 3. GitHub 自动化

`.github/workflows/` 三个工作流：

- `deploy-web.yml` —— 推 main 自动部署 GitHub Pages
- `build-android.yml` —— 打标签自动出 APK/AAB，支持签名配置
- `build-ios.yml` —— macOS runner 云构建 IPA，解决 Windows 无法构建 iOS 的问题

### 4. 操作文档

- `docs/02-GitHub与自动构建.md` —— 从配置 Git 身份到推送、开 Pages、打标签的完整步骤，含常见报错排查
- `docs/03-发布上架操作手册.md` —— Android 和 iOS 双端完整上架流程，**特别说明了证书创建不需要 Mac、只有编译打包需要**，并给出了 Windows 上用 OpenSSL 生成 iOS 证书的具体命令

### 5. 辅助脚本

- `scripts/gen-icons.mjs` —— 纯 Node 生成 PWA 图标，无需下载素材（手写 PNG 编码，已验证可生成 192/512 图标）
- `scripts/setup-git.sh` —— 交互式 Git 初始化引导，含敏感文件检查

---

## 三、验证结果（2026-09-01 复核）

| 检查项 | 命令 | 结果 |
|---|---|---|
| 生产构建 | `npm run build` | ✅ 通过，354 模块，10s |
| 类型检查 | `npm run typecheck` | ✅ 无错误 |
| 页面可访问 | 预览服务 | ✅ HTTP 200 |
| PWA 离线包 | 构建产出 | ✅ 26 个预缓存条目，459 KiB |
| Service Worker | 构建产出 | ✅ `sw.js` + workbox 已生成 |

产物体积：首屏 JS 约 48 KiB（gzip），Vant 全量 CSS 约 79 KiB（gzip）。

---

## 四、环境现状

| 项目 | 状态 |
|---|---|
| Node.js | v24.14.0 ✅ |
| npm | 11.9.0 ✅ |
| Git | 2.55.0 ✅ |
| 依赖安装 | ✅ 325 个包，完整 |
| **Git 身份配置** | ❌ **未配置，需要用户先设置** |
| GitHub 仓库 | ❌ 未创建 |

### 本次发现的环境问题：Vite 开发服务器会被删除守卫拦截

**症状**：`npm run dev` 启动后约 30 秒崩溃，报
`[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":58,"threshold":50}`

**根因**：Vant 组件是按需引入的，Vite 在运行时才发现新依赖并触发重新预构建，
提交时要清理 `node_modules/.vite/deps_temp_*` 临时目录（58 个文件），超过守卫 50 个的阈值。

**已做的修复**：在 `vite.config.ts` 里加了 `optimizeDeps.include`，把用到的 Vant 组件
写进预构建列表，让预构建在启动时一次完成，而不是运行时零碎触发。已验证构建和类型检查仍通过。

**重要说明**：这是 WorkBuddy 沙箱环境的限制，**不影响你自己电脑上跑 `npm run dev`**。
你在自己的终端里开发是正常的。需要预览效果时，在 `app/` 下执行 `npm run preview` 即可（用生产构建产物，不触发预构建）。

---

## 五、用户的下一步

1. 打开 `app/src/views/LedgerView.vue`，了解一个完整功能模块的写法
2. 照此结构把自己的功能加进去
3. 修改 `app/capacitor.config.ts` 里的 `appId` 和 `appName`（**上架后无法修改，必须提前定好**）
4. 修改 `app/public/content/categories.json` 换成自己的内容
5. `npm run preview` 验证效果（开发用 `npm run dev`，有热更新）
6. 按 `docs/02-GitHub与自动构建.md` 连接 GitHub，或运行 `bash scripts/setup-git.sh` 走交互式引导

---

## 六、需要用户决策的事项

> **当前进度卡在第 0 项**。前三项不需要你动手做，只需要把信息给我，我就能继续推进到阶段二。

0. **Git 提交身份**（阻塞项，必须提供）
   - 提交时显示的名字，例如 `Zhang San`
   - 提交时显示的邮箱，**必须是 GitHub 注册邮箱**，否则贡献记录不会关联到你的账号
   - 你的 GitHub 用户名
   - 这三项写进提交记录后是永久的，且邮箱关联账号归属，我不代为猜测，需要你本人确认

1. **应用包名（appId）** —— 建议格式 `com.你的名字.应用名`，需在首次上架前确定
2. **是否公开仓库** —— 公开仓库 GitHub Actions 免费无限，私有仓库每月 2000 分钟额度
3. **个人还是公司开发者账号** —— 公司账号需要邓白氏编码，申请需数日
4. **具体要实现的功能** —— 当前交付的是通用骨架 + 记账示例模块，需用户明确业务功能后替换

---

## 七、风险提示

1. iOS 打包必须有 macOS 环境，已通过 GitHub Actions 云 Mac 方案解决，但需用户配置 5 个 Secrets
2. Android 签名密钥（.jks）一旦丢失将无法再发布更新，必须多处备份
3. Apple 审核拒审率不低，工具类应用易因"功能过于简单"被拒，功能需做扎实
4. 网页套壳应用需具备实质离线能力，否则 Apple 会拒审——脚手架已在架构层面处理
