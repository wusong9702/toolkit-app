# 记忆快照与接续清单（2026-09-01）

> **这是跨设备记忆核心文件**。换电脑后 `git clone` 本项目，让 WorkBuddy 读这一篇即可完整接上。
> 配套文档：00 导航 / 01 技术方案 / 02 自动构建 / 03 上架手册 / 04 密码本功能 / 05 本地验证。

---

## 一、项目一句话现状

一个**纯密码管理器** App：Vue 3 + Vant 4 + Pinia + Capacitor 7 一套代码，
输出网页版 / iOS / Android。数据端到端加密（PBKDF2 15 万次 + AES-256-GCM），
支持 WebDAV 云同步（坚果云/Nextcloud/NAS）。主功能=密码管理，**记账功能已删除**。

- 包名：`com.mimaxing.toolkit`（**上架后永久不可改**，已和用户确认）
- 应用名：密码本
- 仓库：`git@github.com:wusong9702/toolkit-app.git`（公开）
- GitHub 用户名：`wusong9702`（注意：用户口述是 wusong，实际 GitHub 账号是 wusong9702）
- 提交身份：name=`wusong`，email=`55779666+wusong9702@users.noreply.github.com`

---

## 二、用户画像（给"我"看的，跨项目也适用）

- **纯新手**，自称"小白"，要"用一句话讲清楚"，不怕慢就怕卡住
- 设备：**只有 Windows 电脑**，无 Mac（iOS 构建靠 GitHub Actions macos-15 云构建）
- 语言：中文
- 习惯：**先手动验证再上传**；不喜欢被催促，要尊重"先让我试试"的意愿
- 关心：数据隐私（本地加密、主密码不可找回）、能自己在家折腾

---

## 三、已实现功能（指向 04/05 文档）

主页=密码本：② 顶部搜索栏（搜名称/密码/备注/标签/分组）③ 右上角设置 ④ 收藏区
⑤ 分组宫格（含条目数）⑥ 悬浮新增。全部页：分组筛选、置顶/上移/下移、失效时间标记。
设置页：WebDAV 云同步（**测试连通** + 三选删除：仅云端/云端+本地/取消）。

---

## 四、已知坑速查（血泪经验）

1. **npm install 被 sandbox 守卫拦截**：根因多是 cdn 超时导致 npm 回滚删文件超阈值。
   用 `npm install --registry=https://registry.npmmirror.com --fetch-timeout=300000 --fetch-retries=8`
2. **Vite dev 运行时崩溃（同守卫）**：`vite.config.ts` 的 `optimizeDeps.include` 写死用到的 Vant 组件
   （含 action-sheet/date-picker/time-picker/search/empty/tag 等）
3. **Capacitor 7 删了 `bundledWebRuntime`**，写了编译报错——已删
4. **路由必须 hash 模式**（`createWebHashHistory`），Capacitor 是 file:// 协议，history 模式白屏
5. **PWA 改完代码要换端口预览**：Service Worker 缓存旧版，同端口刷新测到的是老代码（换新端口=新 origin）
6. **curl 退出码 23 是误报**（写 /dev/null 导致），看 http_code 别看退出码，别接 `|| echo`
7. **Bash 后台进程随命令结束被回收**：预览服务要用 `run_in_background: true` 启动，否则 present_files 报"无服务"
8. **Vant 4 没有 `showActionSheet` 函数式 API**（只有 Toast/Dialog/Notify/ImagePreview），
   ActionSheet 必须用 `<van-action-sheet v-model:show>` 组件
9. **WebDAV 坚果云网页版被 CORS 拦截**（浏览器直连失败），打包成 App 后无此限制；Nextcloud/NAS 没事
10. **剪贴板预览面板会静默失败**：已做 `navigator.clipboard → execCommand` 双级降级（含 iOS `setSelectionRange`）
11. **`.git/index.lock` 卡 add/commit**：多为 0 字节空残留，`rm -f` 直接删即可（别折腾强制删除）
12. **Android 工作流签名未接线**：写入 keystore.properties 但 Capacitor 生成的 build.gradle 没读（待修）
13. **iOS 工作流问题**：缺 `security set-key-partition-list`、`-allowProvisioningUpdates` 判断写反（待修）

---

## 五、待办清单（按优先级）

- [ ] **GitHub Pages 已开启后 Re-run 验证**（用户已在网页端设置好，应已可访问）
- [ ] 密码本新功能已推送 GitHub ✅（2026-09-01 完成，778326b..2586c15）
- [ ] 修 Android 签名接线（keystore.properties → build.gradle）
- [ ] 修 iOS 工作流（密钥串分区 + allowProvisioningUpdates 判断）
- [ ] 真机 WebDAV 同步实测（坚果云需 App 版；Nextcloud 可网页测）
- [ ] 可选：剪贴板自动清空、多设备冲突合并、触屏拖拽排序

---

## 六、换电脑后接续步骤（给用户的）

```powershell
# 1. 装好 Node 22 + Git，配置 SSH 或准备 GitHub Token
# 2. 拉代码
git clone https://github.com/wusong9702/toolkit-app.git
cd toolkit-app\app

# 3. 装依赖（国内镜像）
npm install --registry=https://registry.npmmirror.com

# 4. 起服务
npm run dev
# 浏览器开 http://localhost:5173 ，手机同 WiFi 用终端显示的 Network 地址
```

进 WorkBuddy 后直接说：**"接着上次的密码本项目，先读 `docs/06-记忆快照与接续清单.md`"**，
我读完就知道全部上下文，无缝接着干。

> 若 WorkBuddy 本机身份文件（SOUL/IDENTITY/USER）没自动跨设备同步，
> 告诉我一声，我重新初始化身份即可，不影响项目本身。
